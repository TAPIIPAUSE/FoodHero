import 'package:flutter/material.dart';

class InterOrganization extends StatelessWidget {
  const InterOrganization({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Inter Organization"),
      ),
    );
  }
}