import 'package:flutter/material.dart';

class ConsumedFood extends StatelessWidget {
  const ConsumedFood({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Consumed Food"),
      )
    );
  }
}